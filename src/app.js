import Vue from 'vue';

document.addEventListener('DOMContentLoaded', () => {
  console.log("we're awesome");
  new Vue({
    el: '#app',
    data: {
      countries: ["Scotland", "Canada"]
    },
    methods: {

    }
  });
});
